{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd ci-dashboard && npm ci && npm run build",
  "outputDirectory": "ci-dashboard/dist",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
